name: "DEPLOY DEV"

on:
  push:
    branches:
      - develop

permissions:
  id-token: write
  contents: read

jobs:
  terraform:
    uses: ./.github/workflows/terraform.yaml
    with:
      environment: "prod"
      aws-assume-role-arn: ${{ secrets.AWS_ASSUME_ROLE_ARN_DEV }}
